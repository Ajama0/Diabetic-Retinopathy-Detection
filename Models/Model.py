import os
import cv2
from tqdm import tqdm
import pickle
import numpy as np
import matplotlib.pyplot as plt
import torch
import torchvision
import pandas as pd


#once data is processed set to False to save as pickle file
REBUILD_DATA = True
#path to the trainset images
path = "Dataset/raw/train_images"
#path to 
train_csv_path = "Dataset/raw/test_images"
IMG_SIZE = 224
training_data = []















 
